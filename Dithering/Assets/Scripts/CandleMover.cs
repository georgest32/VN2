using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CandleMover : MonoBehaviour
{
    Vector3 startPos;
    float timer;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    startPos = transform.position;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    transform.Translate(new Vector3(startPos.x, startPos.y * Mathf.Sin(timer)/5, startPos.z));
    //    timer++;
    //}
}
